# 实验1 网页制作语言 HTML 的应用

## 一、实验名称

网页制作语言 HTML 的应用

## 二、实验目的

1. 了解并学会使用常用的 HTML 语言标记。
2. 利用 HTML 编辑器建立 HTML 文档，制作简单网页。
3. 理解页面全局刷新、局部刷新的概念。
4. 掌握使用表格展现数据，以及使用表格或框架实现页面布局的方法。

## 三、实验环境

- 操作系统：Windows
- 开发工具：Visual Studio Code
- 运行环境：浏览器
- 使用技术：HTML
- 限制条件：本实验未使用 CSS 和 JavaScript。

## 四、实验内容与实现

### 1. 总入口页面

根目录的 `index.html` 作为实验总入口，通过表格将 `exp1_2`、`exp1_3` 和 `exp1_4` 三个实验部分集中展示。用户点击对应链接后，可以进入相应题目的首页。

主要实现方式如下：

- 使用 `<table>`、`<tr>` 和 `<td>` 完成入口页面的居中布局。
- 使用 `<a href="...">` 实现三个实验目录之间的跳转。
- 每个子目录均以 `index.html` 作为首页。

### 2. exp1_2：使用超链接切换课表页面

`exp1_2/index.html` 为课表首页，页面中包含第三周课表的标题、学期与日期信息、全周及星期一至星期日的超链接，以及全周课程表。

该部分的实现特点如下：

- 使用 `<a>` 标签分别链接到 `overall.html`、`Monday.html`、`Tuesday.html`、`Wednesday.html`、`Thursday.html`、`Friday.html`、`Saturday.html` 和 `Sunday.html`。
- 使用 `<table border="1">` 展示课程名、教室、节次和教师等数据。
- 每个星期页面均为独立 HTML 文档，点击链接后整个页面刷新并显示相应日期的课表。
- 星期一页面当前显示“无课”，其他日期页面根据课程安排显示相应课程。

这种实现对应实验要求中的第一种课表页面：通过超链接访问不同页面，结构清晰，便于理解页面整体跳转。

### 3. exp1_3：使用框架进行页面布局

`exp1_3/index.html` 使用 `<frameset>` 将页面划分为上下两部分：

- 上方区域加载 `title.html`，用于显示课表标题、学期和日期。
- 下方区域继续划分为左右两部分。
- 左侧区域加载 `nav.html`，显示全周和星期一至星期日的导航链接。
- 右侧区域加载 `overall.html`，作为课表内容显示区域。

`nav.html` 中的链接通过 `target="content"` 指定内容加载到右侧框架中，因此点击左侧导航时，只更新右侧课表区域，不需要刷新整个页面。

这种结构体现了页面局部刷新的思想，也将标题、导航和内容拆分成多个相互配合的 HTML 文件。

### 4. exp1_4：框架布局页面

`exp1_4` 目录的页面结构与 `exp1_3` 基本相同，但入口页按照实验要求改用 `iframe`：

- `title.html` 负责显示标题信息。
- `nav.html` 负责显示左侧导航。
- `overall.html` 及各星期页面负责显示具体课表。
- `index.html` 使用表格完成上下、左右分栏，并在各区域中嵌入 `iframe`。
- 顶部 `iframe` 加载 `title.html`，左侧 `iframe` 加载 `nav.html`，右侧 `iframe` 加载 `overall.html`。
- 导航链接使用 `target="content"`，使页面内容在右侧区域更新。

该实现使用 `<iframe>` 替代传统的 `<frameset>` 和 `<frame>` 标签，在保持标题、导航和课表内容不变的同时，实现了页面分区和右侧内容局部更新，符合实验要求。

## 五、页面结构

```text
exp1/
|-- index.html                 实验总入口
|-- exp1_2/
|   |-- index.html             超链接课表首页
|   |-- overall.html           全周课表
|   |-- Monday.html ...        每日课表页面
|-- exp1_3/
|   |-- index.html             frameset 页面布局入口
|   |-- title.html             顶部标题
|   |-- nav.html               左侧导航
|   |-- overall.html           默认内容
|   |-- Monday.html ...        每日课表页面
|-- exp1_4/
    |-- index.html             框架布局入口
    |-- title.html             顶部标题
    |-- nav.html               左侧导航
    |-- overall.html           默认内容
    |-- Monday.html ...        每日课表页面
```

## 六、实验结果

1. 打开根目录 `index.html` 后，可以看到 `exp1_2`、`exp1_3` 和 `exp1_4` 三个入口链接。
2. 进入 `exp1_2` 后，点击“全周”或具体星期链接，可以跳转到不同的课表页面。
3. 进入 `exp1_3` 后，页面被分成标题区、导航区和内容区，点击左侧导航时右侧内容区域发生变化。
4. 进入 `exp1_4` 后，可以看到与 `exp1_3` 类似的分栏课表页面，能够完成标题、导航和内容的分区显示。
5. 课表数据能够通过 HTML 表格正确排列，课程名、教室、节次和教师等信息清晰可见。

## 七、问题分析与改进

1. 当前代码使用了较多 HTML 的表现属性，例如 `width`、`height`、`align`、`cellspacing`、`cellpadding` 和 `<font>`。这些写法可以完成实验要求，但在现代 HTML 中已经不推荐使用，后续可使用 CSS 统一控制页面样式。
2. `exp1_3` 使用了 `<frameset>`，该标签在 HTML5 中已被废弃；`exp1_4` 已改用 `<iframe>` 实现相同的分区和局部页面加载效果。
3. 部分页面的表格列数不完全一致，例如全周页面包含四列，而星期页面包含五列表头。后续可以统一表头设计，或者根据页面用途明确区分“节次”和“时间”字段。
4. 各页面中课表和导航代码存在重复，后续可以在实际项目中使用模板、组件或后端数据生成方式减少重复代码。本实验由于限定只使用 HTML，保留了独立页面的实现方式。

## 八、实验总结

通过本次实验，掌握了 HTML 文档的基本结构，以及标题、段落、超链接、表格和框架等常用标签的使用方法。实验中首先使用超链接实现不同课表页面之间的整体跳转，然后使用框架将标题、导航和内容拆分到不同区域，实现了页面局部更新。

本实验还说明了页面结构设计的重要性：将导航和内容分离后，页面的组织更加清楚，用户操作也更加方便。同时，通过对现有代码的检查可以发现，传统 `frameset` 与实验要求的 `iframe` 并不完全相同，后续应根据具体要求选择正确的 HTML 标签完成页面布局。

## 九、提交文件说明

- 实验报告：`实验报告.md`
- 实验源码：根目录 `index.html` 以及 `exp1_2`、`exp1_3`、`exp1_4` 三个子目录中的 HTML 文件。
- 如需按课程要求提交 Word 文档，可将本报告内容复制到 Word 中，补充姓名、学号、班级和实验日期后保存为 `.docx` 格式。